const Home = () => {
  return <h2 style={homeStyle}>Welcome to Bug Logger</h2>;
};

const homeStyle = {
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  height: "10vh",
  margin: 0,
};

export default Home;
